# Required by docker-compose.yml. Generate a long random value and keep it private.
KITCHENOWL_JWT_SECRET=replace-with-a-long-random-secret

# Optional; the default is the path used by the NAS deployment.
KITCHENOWL_DATA_PATH=/volume1/docker/kitchenowl/kitchenowl_data
